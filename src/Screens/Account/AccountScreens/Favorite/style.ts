import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  btnsview: {
    marginTop: 10,
  },
  main: {
    flex: 1,
    backgroundColor: '#fff',
  },
});

export default styles;
